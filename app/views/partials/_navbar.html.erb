<div class="navbar">
  <div class="logged-user">
    <p><strong> <%= current_user.name %> <%= current_user.lastname %></strong></p>
  </div>
  <div class="home">
    <%= link_to "Casita", users_path %>
  </div>
  <div class="students">
    <%= link_to "Estudiantes", students_path %>
  </div>
  <div class="logout-button">
    <%= link_to t('home.cerrar_sesion'), logout_path, class:'logout-btn' %>
  </div>
</div>